{
    "name": "grpc-client",
    "description": "A simple REST API to demonstrate the power of gRPC",
    "version": "1.0.0",
    "main": "src/app.ts",
    "scripts": {
        "start": "tsnd --no-notify --respawn --transpile-only -- ./src/app.ts",
        "build": "tsc -p tsconfig.json"
    },
    "license": "MIT",
    "dependencies": {
        "@grpc/proto-loader": "^0.5.4",
        "dotenv": "^8.2.0",
        "express": "^4.17.1",
        "grpc": "^1.24.3"
    },
    "devDependencies": {
        "@types/express": "^4.17.7",
        "ts-node-dev": "^1.0.0-pre.51",
        "typescript": "^3.9.6"
    }
}
